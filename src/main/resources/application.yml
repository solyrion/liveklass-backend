spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://localhost:3306/liveklass?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: liveklass
    password: liveklass1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
