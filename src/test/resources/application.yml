spring:
  datasource:
    url: jdbc:mysql://localhost:3307/liveklass_test?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: liveklass
    password: liveklass1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
